random := 17
WriteLine("Guess my number between 1 and 100")
guess := (int)ReadLine()
while guess != random:
    if guess > random:
        WriteLine("Too high")
        guess = (int)ReadLine()
    unless guess < random:
        WriteLine("Too low")
        guess = (int)ReadLine()
    else:
        WriteLine("You got it!")
        break
